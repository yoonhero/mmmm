Level1
`file` -> show property (-i MIME -L symlink)
: fingerprints located in /usr/share/misc/magic.mgc

`du` -> -h -s -d 
: sums all the st_blocks values

`find` -> -name -type -size -mtime -user -perm -empty -exec

Level 2
I can access dashed filename with ./-
-: refers to STDIN/STDOUT
(cd -: previous dir / cat -: keyboard)

Level 3
space = "\ "

Level 4
ls -la

Level 5
find -type f -exec file {} \; | grep -i text -B1

Level 6
find -type f -size 1033c ! -executable

Level 7
find . -user bandit7 -group bandit6 -size 33c 2>&1 | grep -v "Permission denied" | grep -v "No such"

Level 8
cat < data.txt | grep "millionth"

Level 9
sort data.txt | uniq -u -> after sort call!
sort data.txt | uniq -c | grep "1 "

Level 10, 11
strings data.txt | grep -E "=+\s+"
base64 -d data.txt

strings: binary into printable
base64: binary into ASCII(transferring attachments through text-base protocol)
xxd, hexdump: hex,ascii,octa etc

Level 12
cat data.txt | tr 'A-Za-z' 'N-ZA-Mn-za-m'

Level 13
tar: make dump, not compress(-xf)
gzip: real compress(-d)
bzip2: slow but good(-d)

Level 14
ssh with private key -> ssh -i (0644 is too high for this action)

Level 15
telnet localhost 30000

Level 16
openssl s_client localhost:30001

Level 17
nmap -sT -p 31000-32000 localhost -> checking TCP conn(sS: half / sU: UDP)
nmap --script ssl-cert -p <port> localhost -> check TLS/SSL availability
openssl s_client -connect localhost:<port> -quiet -> why -quiet? DONE/RENEGOTIATING/KEYUPDATE msg

Level 18
diff old new (< old / > new)
diff -u old new (git style)
  
Level 19
ssh user@host <commmand> -> more priority than .bashrc?(it is non-interactive shell mode!)

Level 20
./bandit20-do it
: uid=11020(bandit20) / gid=110190(bandit19)

Level 21
echo -n '0qXahG8ZjOVMN9Ghs7iOWsCfZyXOUbYO' | nc -l -p 1234 &
./suconnect 1234

You can test raw ping-pong with this command, `nc` and `curl`. When you open some port with nc, you can answer to GET request like following msg.


```
HTTP/1.1 200 OK
Content-Type: text/plain
Content-Length: 13

Hello, world!
```

Level 22, 23, 24
cron, crontab / bash script

Level 25

```bash
#!/bin/bash

for ((i=0; i<10000; i++)); do
  printf "gb8KRRCsshuZXI0tUuR6ypOFjiZbf3G8 %04d\n" "$i" >> poss.txt
done

cat poss.txt | nc localhost 30002 > result.txt
```

Level 26, 27

I need more... (they re genius)

Level 28, 29, 30, 31, 32

do you know a Git?

- git branch -a : bare, detached from origin, or cloned with minimal refs.
- git show-ref / git show <ref> : ref works just like pointer of commit history.
- remote rejection! / git add -f <file>

Level 33

I WANT TO BE LOWER.